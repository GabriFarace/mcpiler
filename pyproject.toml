[project]
name = "mcpiler"
version = "0.1.0"
description = "Compile a bounded FastAPI fixture into human-reviewable MCP interface artifacts"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "langchain-openai>=1.4.1,<2",
    "pydantic>=2.13.4,<3",
]
